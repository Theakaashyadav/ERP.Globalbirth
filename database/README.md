# Database

This app uses MongoDB Atlas through Mongoose.

Collections and indexes are created from these schema files when the server connects:

- `src/server/models/Employee.js`
- `src/server/models/AttendanceRecord.js`

No SQL import is required.
